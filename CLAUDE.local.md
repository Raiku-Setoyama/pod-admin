# 開発ガイド — `/spec` & `/ship` の使い方

## 指針
- 新規セッションの場合、ユーザーからの依頼に対して、基本的に /spec にて、仕様検討から始める。
- ユーザ＝からの依頼に既に仕様が決まった issue が明記されている場合は /ship で全自動開発を開始する。
- ユーザーから全自動開発を行わない旨のメッセージや、調査タスクなどは例外として、/spec や /ship を実行しない。

## 全体の流れ

```
/spec <要望>  →  対話で仕様作成  →  .claude/specs/FEAT-XXXX.yaml に保存
                                         ↓
/ship --spec FEAT-XXXX  →  設計 → 実装 → テスト → PR作成（全自動）
```

---

## 1. `/spec` — 仕様を作る

```
/spec ユーザーのプロフィール画像をアップロードできるようにしたい
```

- Claude が要望を分析し、不明点を **1〜3問** 質問する
- 回答すると仕様ドラフトが提示される（スコープ・受け入れ基準・制約など）
- 「OK」「進めて」で確定 → `.claude/specs/FEAT-XXXX.yaml` に保存
- 細かいことを任せたい場合は「任せる」と伝えればOK

**ポイント:** `/spec` は仕様作成のみ。実装は行わない。

---

## 2. `/ship` — 全自動で実装〜PR作成

```
/ship --spec FEAT-0001
```

以下の3フェーズが **人間の承認なしで** 一気に実行される:

| Phase | 担当 | 内容 |
|-------|------|------|
| 1 | planner | アーキテクチャ判断・実装設計・テスト生成 |
| 2 | builder | 実装 → コード品質改善（`/simplify`）→ 5段階の品質チェック |
| 3 | ship | コミット → プッシュ → PR作成 |

### 自動リトライ

- 品質チェックに失敗した場合、builder が **最大3回** 自動修正を試みる
- 3回失敗 or 環境エラーの場合のみ、人間に報告して停止

### 実行環境

- **Worktree** で作業するため、メインブランチは汚れない
- **Docker** 内でテスト・ビルドを実行（ローカル環境に影響なし）
- 終了時に `docker compose down` で自動クリーンアップ

---

## 典型的なワークフロー

```bash
# 1. 仕様作成（対話）
/spec ログイン時にSlack通知を送りたい

# 2. 質問に回答 → 仕様確定

# 3. 全自動実装
/ship --spec FEAT-0001

# 4. PRが自動作成される → レビュー
```

---

## 注意事項

- `/ship` には必ず `--spec` で仕様ファイルを指定する（指定なしはエラー）
- `/spec`・`/ship` 実行時のみ ADD方式のルール（テスト戦略・品質チェック等）が適用される
- 通常の Claude Code 利用時にはこれらのルールは適用されない

---

## GitHub PR作成ルール

`gh pr create` でPRを作成する際は、必ずPR本文の末尾に以下を追加してください：

```
---
🤖 Claude Code Session: https://claude.ai/code/sessions/SESSION_ID_HERE
```

（`SESSION_ID_HERE` は現在のセッションIDに置き換える）