# WORKFLOW — 起票から完了まで

**この 1 枚を読めば運用を始められます。** 規約の正本は `AGENTS.md` です。

---

## 3 行でいうと

1. 出た話は**すべて要件（`REQ-XXXX`）として起票**する。やると決まっていなくてよい。
   打ち合わせで出た話も、Slack で流れた話も、行き先は同じ 1 か所である。
2. 「やるか」と「何をもって完了とするか」は**同じ PR で人間が決める**。
   根拠が入力に揃っていれば、その PR は起票 PR そのものになる。
3. **要件がそのまま作業 ID**。起票した ID のまま実装 PR になる。Issue もタスクも作らない。
4. **完了したものが壊れていたら、それは要件ではなく不具合（`BUG-XXXX`）**。
   約束はすでにしているので、採否を決め直さない。**直すのが既定**である。

---

## 1 つの要件のライフサイクル

```
話が出る（打ち合わせ / Slack / 口頭 / 実装中の気づき）
  │
  ├─ /meeting-intake  ────────────→ 【判断①】起票 PR
  │  または                            「出た話と合っているか」
  │  /requirements-intake              ＋「やるか」＋「何をもって完了とするか」
  │     REQ-0042 を起票
  │     status:   not-started
  │     priority: 根拠があれば must / future / wont を提案
  │              なければ undecided
  │     must を提案するなら 受入基準・area・depends_on もここで揃える
  │
  └─ /implement REQ-0042 ─────────→ 【判断②】実装 PR
        status: not-started              「マージしてよいか」
             → in-progress → done      ← マージが完了の承認
```

**人間がやることは、この 2 つの PR をマージするかどうかだけです。**
**要件 1 件について、レビューは起票と実装の 2 回で済みます。**

ライフサイクルの外に、時間が経つと溜まるものを回収する系統があります。

```
  /docs-audit（週 1）───────────→ 監査 PR
     決まっていない論点（proposed の ADR）/ 実装状態のズレ
     30 日以上動いていない要件 / 参照切れ・矛盾・領域の劣化
```

**WBS はコミットしません。** 要件からの投影なので、必要なときに生成します。

```bash
python3 scripts/export-wbs.py               # dist/wbs.md を生成
python3 scripts/export-wbs.py --actionable  # 着手可能な要件だけを出す
```

## 1 つの不具合のライフサイクル

**判断ゲートが 1 つ少なくなります。** 採否（やるか）はすでに決まっているからです。

```
完了した機能が期待どおり動かない
  │
  ├─ /defect-intake ──────────────→ 【判断①】起票 PR
  │     再現を確認                     「不具合として合っているか」
  │     BUG-0003 を起票                 ← 種別の判定はここで覆せる
  │     priority: must   ← 既定。要件と逆で、下げるときだけ判断が要る
  │     status:   not-started
  │     defect_of: [REQ-0009]  ← 違反している受入基準を持つ文書
  │
  │  （判断②はありません。「直す」はすでに約束済みです）
  │
  └─ /implement BUG-0003 ─────────→ 【判断②】修正 PR
        再現するテストを先に書く         「マージしてよいか」
        → 原因を特定 → 直す
        status: → done
```

**要件は `done` のまま戻しません。** `done` は「マージ時点で全基準を満たした」という
履歴の記録であり、今も満たしている保証ではありません。
今の健全性は「未修正の不具合があるか」で表し、これは `defect_of` の逆引きで導出されます
（`/status-report` と顧客向け要件タブが出します）。

**打ち合わせは前提ではありません。** 起票の入口が 2 つあるだけで、以降の流れは同じです。

| 起票の材料 | 入口 | 議事録 |
|---|---|---|
| 文字起こしのある打ち合わせ | `/meeting-intake` | 作る（`MTG-YYYY-MM-DD`） |
| Slack のスレッド・口頭のメモ・箇条書き | `/requirements-intake` | 作らない。出典は要件の `## 経緯` に書く |
| **不具合の報告**（バグ・エラー・障害） | **`/defect-intake`** | 作らない。`BUG-XXXX` として起票される |

要件の `source`（議事録 ID）は**打ち合わせ由来でなければ空（`[]`）で構いません。**
`scripts/docs-lint.py` も CI も、議事録の存在を要求しません。

要件は 2 つのフィールドで表されます。詳細は `docs/01-requirements/index.md`。

| 軸 | フィールド | 値 | 誰が決めるか |
|---|---|---|---|
| 採否 | `priority` | `undecided` / `must` / `future` / `wont` | **人間（PR マージ）** |
| 実装 | `status` | `not-started` / `in-progress` / `done` / `on-hold` | エージェント |

---

## 運用サイクル

チームの形に合わせて 2 通りあります。**違うのは判断の場を「打ち合わせ」に置くか
「PR」だけに置くかで、判断①②の中身は同じです。**

### A. 定例が週 1 回ある場合

```
月曜  /meeting-agenda 2026-08-04
      → アジェンダ PR + 顧客送付用テキスト。前日中に顧客へ送る

火曜  文字起こしを docs/03-meetings/raw/ に置く
      /meeting-intake 2026-08-04
      → 議事録 + 要件起票 PR（合意が明確なものは must を受入基準つきで提案）
      → 【判断①】記録が合っているか＋採否の提案でよいかを見てマージ
      → ここで must が通れば、そのまま実装へ進める

水〜金 /implement REQ-0042
      → 実装 + 品質検証 + PR
      → 【判断②】レビューしてマージ（要件が done になる）
      （複数要件を並行させる場合は git worktree で分離）

随時  不具合が出たら /defect-intake → /implement BUG-XXXX
      定例を待たない。採否はすでに決まっている

金曜  /status-report client     → 顧客向け週次報告
月末  /client-export            → Excel(4タブ) / CSV / 要件定義書(Word)
週次  /docs-audit               → 論点の回収・状態の突合・放置と参照切れの検出
```

### B. 定例を持たない場合（少人数チーム向け）

打ち合わせに紐づく `/meeting-agenda` と `/meeting-intake` を使いません。
**`/requirements-intake` が唯一の入口になります。**

```
随時  話が出たその場で /requirements-intake
      「この Slack のスレッドを起票して」でよい
      → 起票 PR（依頼が明確なものは must を受入基準つきで提案）
      → 【判断①】起票内容と採否の提案を見てマージ
      → ここで must が通れば、そのまま実装へ進める

随時  不具合が出たら /defect-intake
      → 再現を確認して BUG-XXXX を起票（priority: must）
      → 【判断①】不具合として合っているかを見てマージ
      → 採否は既に決まっているので、そのまま /implement BUG-XXXX へ

随時  /implement REQ-0042
      → 実装 + 品質検証 + PR
      → 【判断②】レビューしてマージ（要件が done になる）

週 1  /docs-audit
      → proposed の ADR が全件ここに集まる          ← 論点の回収はここだけ
      → 実装状態のズレ / 30 日以上動いていない要件 / 参照切れ・矛盾
      → 提案をレビューしてマージ

週次  /status-report            → 進捗の把握
随時  python3 scripts/export-wbs.py  → dist/wbs.md（コミットしません）
```

**`/docs-audit` を止めると、この形は破綻します。** 定例がある場合は判断待ちが
`/meeting-agenda` 経由で議題に上がりますが、定例がなければ
**`proposed` の ADR を人間の前に運ぶ経路がここしかありません。**
論点は起票の入力に出てこないので、**時間駆動でしか拾えません。**
週 1 回で回らなければ隔週でも構いませんが、**動かさない期間を作らないでください。**

初回だけ注意点があります。**領域（`area`）の語彙は初回の `/requirements-intake` が
起票の材料から提案します。** 語彙が `common` と `unassigned` だけのままだと、
`must` に上げる時点で `docs-lint` が落ちて実装に進めません（→ `docs/00-charter/areas.md`）。

---

## 各段階で何が起きるか

### ① 打ち合わせ前 — `/meeting-agenda [日付]`

<!-- 定例がある場合だけの段階です。持たないチームは②から読んでください -->

判断待ちの要件（`priority: undecided`）、決まっていない論点（`proposed` の ADR）、`[NEEDS-DECISION]`、滞留 PR、前回の宿題を集めます。

議題は**「決めてほしいこと」が先頭**に並び、各議題に「判断が出ないと何が止まるか」が付きます。

### ② 起票 — `/meeting-intake [日付]` または `/requirements-intake`

打ち合わせを取り込むなら前者、Slack のスレッドや口頭のメモから起こすなら後者です。
**違いは議事録を作るかどうかだけで、起票のルールは同じです。**

いずれも**出た話がすべて起票**されます。行き先は 3 つだけです。

| 出た話 | 行き先 |
|---|---|
| 「〜したい」「自動化できそう」「これだと困る」 | 要件（`REQ-XXXX`） |
| **「承認は 2 段階か 3 段階か」** | **ADR（`status: proposed`）** ← 論点は要件にしない |
| **「先週入れたあれ、動いていない」** | **不具合（`BUG-XXXX`、`priority: must`）** ← 下記 |

**不具合かどうかは「既存の受入基準に違反しているか」で決まります**（→「迷ったときの判断」）。
打ち合わせでこの判定を結論づけません。瑕疵か仕様変更かは金額に直結するので、
議事録には**顧客が何と言ったか**を書き、判定はこちらの見解として PR 本文に出します。

**採否（`priority`）は、根拠が入力にあればここで提案されます。**
提案してよいのは、**根拠を入力から一行で引用できるときだけ**です。

| 提案 | 入力にこれがあるとき | 併せて必ず埋まるもの |
|---|---|---|
| `must` | 「やる」と言った事実 | **受入基準**、確定した `area` |
| `future` | 「やるが今回ではない」と読める | `decision`、`decided_at`、`area` |
| `wont` | 「やらない」と読める | `decision`、`decided_at`、`area` |
| `undecided` | 上のどれも言えない | — |

**引用できないものを推測で埋めません。** また、合意があっても**受入基準が書けなければ
`must` にしません**（`undecided` に留め、何を確認したいかを PR 本文に書きます）。
「やると決めるなら、何をもって完了とするかも決める」を崩さないためです。
合意した事実は議事録の「決定事項」と要件の `## 経緯` に残るので失われません。

**`depends_on` もここで書きます。** `must` / `future` を提案する要件には、
着手の前提になる既存要件を書きます。全件を横断した洗い直しだけが ④ の担当です。

**実体が複数の要件なら、最初から別の ID で起票します。**「実装が大きいから」では分けません。

PR 本文は「判断していただきたいこと（採否）」と「記録が合っているかの確認」に分かれます。
**採否の提案が 1 件もなければ前者は節ごと省かれる**ので、軽い起票は軽いままレビューできます。
差分を全部読む必要はありません。

### ③ 実装・修正 — `/implement REQ-0042` / `/implement BUG-0003`

**着手可能かどうかはフラグとして保存していません。** その場で判定します。

```
priority ∈ {must, future} ∧ status = not-started ∧ 受入基準が 1 件以上
∧ [NEEDS-DECISION] なし ∧ area ≠ unassigned ∧ depends_on がすべて done
```

以降は `feat/REQ-0042-slug` で実装 → `/quality-gate` → 受入基準を `[x]` → `status: done` → PR（`Refs: REQ-0042`）。
**ステータス更新を別作業でやる必要はありません。**

### ④ 監査 — `/docs-audit`（週 1）

**要件のライフサイクルの外にある系統です。** ② が今回の入力しか見ないのに対し、
ここは**時間が経つと溜まるもの**を回収します。

| 回収するもの | なぜ起票では拾えないか |
|---|---|
| 決まっていない論点（`proposed` の ADR） | 起票の入力に出てこない。時間だけが経つ |
| 実装状態のズレ | マージ済み PR との照合は全件を見ないと分からない |
| 30 日以上動いていない要件 | 引き金が経過時間であり、入力がない |
| 参照切れ・矛盾・陳腐化・領域の劣化 | 同上 |
| 全件を横断した `depends_on` の洗い直し | 起票は今回の要件からの向きしか見ない |

**放置された `undecided` は `wont` の提案に倒します。** 逆向き（`undecided` → `must`）はしません。
**時間の経過は採否の根拠にならない**からです。新しい根拠が出たなら、それは新しい入力なので
`/requirements-intake` で拾います。

### ⑤ 顧客への提出 — `/client-export [review|agreed]`

`docs/` が正本で、資料はそこからの**投影**です。資料を直接編集せず、`docs/` を直して再生成します。

| ファイル | 中身 |
|---|---|
| `dist/プロジェクト管理表.xlsx` | 要件 / 検討事項 / 決定事項 / 不具合の 4 タブ。**1 ファイルで手渡すならこれ** |
| `dist/requirements.csv` | 要件（`must` / `future`）。実装状況の列つき |
| `dist/pending.csv` | 検討事項（`undecided` / `wont`）。判断内容と判断日つき |
| `dist/decisions.csv` | 決定事項・論点（ADR） |
| `dist/defects.csv` | 不具合。対象要件と修正状況つき |
| `dist/要件定義書.docx` | 提出・検収用。目次・改ページつき |

合意版を出したら `git tag -a v1.0-requirements` で版を固定します。

---

## 迷ったときの判断

### 実装が大きい

**行数の上限はありません。** 1 要件 = 1 PR が原則で、分けるのは実装の都合があるときだけです
（スキーマ変更を先に確認したい、機械的置換を切り離したい等）。**「行数が多いから」は理由になりません。**

分けるときは `## 実装メモ` に計画を書き、`status: in-progress` のまま複数 PR で消化して、
**受入基準が全部 `[x]` になった最後の PR が `done` を書きます**。この判断に人間の承認は要りません。

**要件そのものを分けるのは、実体が複数の要件だったときだけ**です。
**起票の時点で分けるのが原則**で、後から気づいた分割は `/docs-audit` が提案します。

### 実装中に判断が必要になった

エージェントは**実装せずに止まります**。出口は 2 つで、どちらもリポジトリに残ります。

- **その場で聞けば済む** → 選択肢と推奨が提示される。回答を要件に反映する PR を先に出す
- **顧客・PM の判断が要る** → **ADR を `proposed` で起票**し、要件に `[NEEDS-DECISION: ADR-XXXX]` を置いて止まる

起票した論点は、**次のどちらかが必ず人間の前に運びます。**

| チームの形 | 回収する経路 |
|---|---|
| 定例がある | `/meeting-agenda` が次回議題に自動で載せる |
| **定例を持たない** | **`/docs-audit` が PR の「判断していただきたいこと（論点）」に出す** |

### 実装中に別の問題を見つけた

直さずに起票します。**スコープを広げません。** 行き先は種別で決まります。

| 見つけたもの | 行き先 |
|---|---|
| 既存の受入基準に違反している | 不具合（`BUG-XXXX`、`priority: must`） |
| 違反していない（新しい要求） | 要件（`REQ-XXXX`、`priority: undecided`） |
| 判定がつかない | **要件に倒す**（`priority: undecided`） |

### 完了した機能が壊れていた

**要件ではなく不具合です。** 判定は 1 つの問いで決まります。

> **既存の受入基準（憲章の `NFR` / `CONSTRAINTS` を含む）に違反しているか。**
>
> - **違反している → 不具合**（`/defect-intake` → `BUG-XXXX`、`priority: must`）
> - **違反していない → 要件**（`priority: undecided`）。新しい約束の話です
> - **判定がつかない → 要件に倒します**

**この線は瑕疵と仕様変更の境界そのものです。** 「書いてある約束を満たしていない」＝瑕疵、
「書いていないことを求められた」＝仕様変更。詳細は `AGENTS.md`「不具合か、仕様変更か」。

**違反している受入基準を引用できないなら、それは不具合ではありません。**

### 全工程を通さなくてよいもの

要件に影響しない軽微な修正（誤字・コメント）と、スキル・CI 自体の改善は直接 PR を出せます。
`Refs: なし（理由）` と書きます。判断基準は**「顧客に説明が必要か」**。詳細は `AGENTS.md`。

---

## 機械が守っていること

規律を人間の記憶に頼っていません。

| 検査 | どこで |
|---|---|
| `must` なのに受入基準が空 | docs-lint → **エラー** |
| `in-progress` / `done` なのに `priority` が `undecided` / `wont` | docs-lint → **エラー** |
| `done` なのに受入基準が未達 | docs-lint → **エラー** |
| `future` / `wont` なのに `decision` が空 | docs-lint → **エラー** |
| 判断済みなのに `area: unassigned` | docs-lint → **エラー** |
| 不具合の `priority` が `undecided` | docs-lint → **エラー**（採否を問う段階が無い） |
| 不具合の `area` が `unassigned` | docs-lint → **エラー**（起票時から確定させる） |
| 1 つの PR が 2 件以上の作業項目（要件・不具合）を実装 | pr-quality-check → **拒否** |
| `/quality-gate` を通さず `git push` | PreToolUse フック → **拒否** |

**`1 要件 → N PR` は可、`1 PR → N 作業項目` は不可。** この非対称が唯一の不変条件です。
**不具合は `1 不具合 → 1 PR` です**（修正が大きくなるなら、複数の不具合が混ざっています）。

---

## もっと詳しく

| 知りたいこと | 読む場所 |
|---|---|
| 規約の正本（ID 体系・領域・2 つの軸・作業の単位） | `AGENTS.md` |
| スキル一覧 | `AGENTS.md`「よく使うスキル」 |
| `priority` と `status` の意味、着手可能の条件 | `docs/01-requirements/index.md` |
| 不具合の扱い（判定・既定値・`done` の意味） | `docs/05-defects/index.md` |
| ビルド・テスト・デプロイ | `AGENTS.md`「プロジェクト固有の設定」 |
