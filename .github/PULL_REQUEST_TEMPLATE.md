## 概要

<!-- 何を、なぜ変更したか。2〜4 行 -->

<!-- Refs は必須。作業 ID は REQ-XXXX。例: Refs: REQ-0012 -->
<!-- 実装 PR に書ける要件 ID は 1 つだけ。要件によらない変更は Refs: なし（理由）と全角括弧で書く -->

Refs:

## 変更内容

-

## 品質検証

<!-- 実装 PR では必須。/quality-gate の出力に従って埋めること -->
<!-- ドキュメントのみの PR ではこのセクションを「該当なし（docs のみ）」に置き換える -->
<!-- 品質ゲートの push フック・CI は段階4で導入予定。それまでは各コマンドを手で通す -->

- [ ] 型チェック: `uv run mypy .`（api） / `npx tsc --noEmit`（web）
- [ ] Lint: `uv run ruff check .`（api） / `npm run lint`（web）
- [ ] テスト: `uv run pytest`（api） / `npm run test:run`（web）
- [ ] `/simplify max` 実行済み
  - 指摘: 件 / 修正: 件

## スクリーンショット

<!-- UI 変更がある場合のみ記載。dev-browser による撮影結果をコミットに含める -->

| 画面 | スクリーンショット |
|------|-------------------|
| <!-- ページ名 --> | <!-- リンク --> |

## レビュー観点

<!-- レビュアーに特に判断してほしい点。「特になし」は避ける -->

-

## ドキュメントの更新

- [ ] 満たした受入基準を `[x]` にした
- [ ] すべて満たしたので `docs/01-requirements/REQ-XXXX.md` の status を `done` にした
      （複数 PR に分けている途中なら `in-progress` のまま）
- [ ] 設計判断があれば ADR を起こした
- [ ] 該当なし

## 発見事項

<!-- 実装中に見つけたが、今回のスコープ外としたもの。priority: undecided で起票した要件 -->

-
